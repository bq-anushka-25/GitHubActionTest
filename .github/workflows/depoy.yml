name: Terraform Code
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

permissions:
  id-token: write
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    environment:
      name: secretForTerraform
    steps:
      - uses: actions/checkout@v3
      - uses: hashicorp/setup-terraform@v2
      - name: 'Az CLI login'
        uses: azure/login@v1
        with:
          # creds: ${{ secrets.AZURE_CREDENTIALS }}
          client-id: '0bad7d0d-ac03-4b71-8d12-7f6433916bea'
          tenant-id: '325cff79-aa80-4049-b025-3696f800ec93'
          subscription-id: '4ded386b-ff71-40c7-9b4c-83cd556a5082'
          enable-AzPSSession: true 
      # - name: 'Terraform init'
      #   id: init
      #   run: terraform init          

      # - name: 'Terraform plan'
      #   id: plan
      #   run: terraform plan
                    
      # - name: 'Terraform apply'
      #   id: apply
      #   run: terraform apply -auto-approve 
          
